<div class="text-slider-wrapper" style="background-color: {{ section.settings.background_color }};">
  <div class="text-slider-container">
    <div class="text-slider" style="animation-duration: {{ section.settings.scroll_speed }}s;">
      {% for block in section.blocks %}
        <div class="text-slide" {{ block.shopify_attributes }}>
          <span class="text-slide-content" style="color: {{ block.settings.text_color }};">{{ block.settings.slide_text }}</span>
          <span class="text-slide-separator">&nbsp;&nbsp;&nbsp;</span>
        </div>
      {% endfor %}
      
      {% comment %} Duplicate the slides for a seamless loop {% endcomment %}
      {% for block in section.blocks %}
        <div class="text-slide">
          <span class="text-slide-content" style="color: {{ block.settings.text_color }};">{{ block.settings.slide_text }}</span>
          <span class="text-slide-separator">&nbsp;&nbsp;&nbsp;</span>
        </div>
      {% endfor %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Sliding Text Banner",
  "settings": [
    {
      "type": "color",
      "id": "background_color",
      "label": "Background Color",
      "default": "#FFFFFF"
    },
    {
      "type": "range",
      "id": "scroll_speed",
      "min": 1,
      "max": 30,
      "step": 1,
      "unit": "s",
      "label": "Scroll Speed",
      "info": "Lower number = faster scroll",
      "default": 15
    }
  ],
  "blocks": [
    {
      "type": "text",
      "name": "Sliding Text",
      "settings": [
        {
          "type": "text",
          "id": "slide_text",
          "label": "Text",
          "default": "Tiny Threads, Big Smiles 😊"
        },
        {
          "type": "color",
          "id": "text_color",
          "label": "Text Color",
          "default": "#000000"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Sliding Text Banner",
      "category": "Text",
      "blocks": [
        {
          "type": "text",
          "settings": {
            "slide_text": "Tiny Threads, Big Smiles 😊",
            "text_color": "#FF5757"
          }
        },
        {
          "type": "text",
          "settings": {
            "slide_text": "Style That Grows With Them",
            "text_color": "#4CAF50"
          }
        },
        {
          "type": "text",
          "settings": {
            "slide_text": "Where Play Meets Fashion",
            "text_color": "#2196F3"
          }
        },
        {
          "type": "text",
          "settings": {
            "slide_text": "Dressing Childhood in Color",
            "text_color": "#9C27B0"
          }
        },
        {
          "type": "text",
          "settings": {
            "slide_text": "Made for Giggles & Adventures",
            "text_color": "#FF9800"
          }
        }
      ]
    }
  ]
}
{% endschema %}

<style>
  .text-slider-wrapper {
    width: 100%;
    padding: 20px 0;
    overflow: hidden;
    position: relative;
  }
  
  .text-slider-container {
    width: 100%;
    overflow: hidden;
    position: relative;
  }
  
  .text-slider {
    display: inline-flex;
    white-space: nowrap;
    animation: slider-scroll linear infinite;
    will-change: transform;
    position: relative;
  }
  
  .text-slide {
    display: inline-flex;
    align-items: center;
    padding: 0 20px;
  }
  
  .text-slide-content {
    font-family: "Caprasimo", serif;
    font-weight: 400;
    font-size: 24px;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }
  
  .text-slide-separator {
    margin: 0 20px;
    font-size: 24px;
  }
  
  @keyframes slider-scroll {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }
  
  @media screen and (max-width: 768px) {
    .text-slide-content {
      font-size: 18px;
    }
    
    .text-slide-separator {
      margin: 0 10px;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    var textSliderElements = document.querySelectorAll('.text-slider');
    
    // Check if we have any text sliders on the page
    if (textSliderElements.length > 0) {
      textSliderElements.forEach(function(slider) {
        // Ensure the animation is playing
        slider.style.animationPlayState = 'running';
        
        // Make sure there's enough content for a smooth loop
        const firstSlide = slider.querySelector('.text-slide');
        if (firstSlide) {
          const sliderWidth = slider.scrollWidth / 2; // Half because we duplicated the slides
          
          // Set animation duration based on content length for smoother scrolling
          const speed = parseFloat(slider.style.animationDuration) || 15;
          slider.style.animationDuration = speed + 's';
        }
      });
    }
  });
</script>